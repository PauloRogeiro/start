<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cabecalho_configuracoes"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:weightSum="1">

    <ImageView
        android:id="@+id/perfil_avatar"
        android:layout_width="118dp"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:scaleType="fitCenter"
        app:srcCompat="@android:drawable/sym_def_app_icon"


     />

    <TextView
        android:id="@+id/perfil_nome"
        android:layout_width="132dp"
        android:layout_height="wrap_content"
        android:padding="@dimen/nav_header_vertical_spacing"
        android:text="Android Studio"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/perfil_avatar"
        android:layout_toEndOf="@+id/perfil_avatar" />

    <TextView
        android:id="@+id/perfil_sobrenome"
        android:layout_width="match_parent"
        android:layout_below="@id/perfil_nome"
        android:padding="@dimen/nav_header_vertical_spacing"
        android:text="Sobrenome"
        android:layout_toRightOf="@id/perfil_avatar"
        android:layout_height="wrap_content"

      />

    <TextView
        android:id="@+id/perfil_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="paulo.ros.oliveira@gmail.com"
        android:padding="@dimen/nav_header_vertical_spacing"
        android:layout_below="@id/perfil_sobrenome"
        android:layout_toRightOf="@+id/perfil_avatar"
         />

</RelativeLayout>
